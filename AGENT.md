# Agent Guidelines

## Pull Requests

Always open pull requests on the **fork repo** (`tamnd/golang-website-vi`), never on the upstream (`golang/website`).

Use `--repo tamnd/golang-website-vi` when running `gh pr create`.
